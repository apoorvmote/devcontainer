# https://taskfile.dev

version: '3'

vars:
  COMMIT_MESSAGE: added cdk-lambda-local

tasks:
  default:
    cmds:
      - git add .
      - git commit -m "{{.COMMIT_MESSAGE}}"
      - git push origin main
